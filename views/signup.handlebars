<!-- Sign Up Form Template -->
<script id="signup-template" type="text/x-handlebars-template">
    <form id="signup-form">
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" required><br>
        
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" required><br>
        
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" required><br>
        
        <button type="submit">Sign Up</button>
    </form>
</script>

<!-- Container to render sign-up form -->
<div id="signup-container"></div>

<script>
    // Data for rendering the template (optional)
    var data = {};

    // Get the HTML content of the template
    var templateSource = document.getElementById("signup-template").innerHTML;

    // Compile the template
    var template = Handlebars.compile(templateSource);

    // Render the template with data and append to the container
    document.getElementById("signup-container").innerHTML = template(data);

    // Handle form submission
    document.getElementById("signup-form").addEventListener("submit", function(event) {
        event.preventDefault(); // Prevent the default form submission

        // Get form data
        var username = document.getElementById("username").value;
        var email = document.getElementById("email").value;
        var password = document.getElementById("password").value;

        // Perform sign-up validation or send sign-up request to server
        // Example:
        // if (username && email && password) {
        //     alert("Sign up successful!");
        // } else {
        //     alert("Please fill in all fields.");
        // }
    });
</script>